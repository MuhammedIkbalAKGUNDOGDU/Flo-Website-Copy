import React from "react";

const swiperelements = ({ link }) => {
  return (
    <div className="swiper-element">
      <img src={link} alt="" />
    </div>
  );
};

export default swiperelements;
